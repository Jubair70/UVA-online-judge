#include<iostream>
#include<cstring>
#include<cstdio>
using namespace std;
int main()
{
    while(scanf("%s%s",s[1],s[2]))
    {
        if(strlen(s[1])>strlen(s[2]))b=strlen(s[1]);
        else b=strlen(s[2])
        for(i=0;i<b;i++)
        {
            for(j=0;j<)
        }
    }
    return 0;
}
